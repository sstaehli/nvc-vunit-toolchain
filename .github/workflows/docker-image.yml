name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
        docker build \
          --file Dockerfile \
          --tag ghcr.io/${{ github.repository_owner }}/nvc-vunit:latest \
          --no-cache .
          
    - name: Publish
      run: |
        docker push ghcr.io/${{ github.repository_owner }}/my-app:latest
